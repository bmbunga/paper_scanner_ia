from send_confirmation_email import send_confirmation_email

if __name__ == "__main__":
    # Mets ici ton email perso pour tester (pas de panique, c'est juste pour vérifier que le mail part)
    send_confirmation_email("mmblaise10@gmail.com")
